apiVersion: 1

providers:
  - name: SOC-SIEM Dashboards
    orgId: 1
    folder: SOC-SIEM
    type: file
    disableDeletion: false
    editable: true
    updateIntervalSeconds: 30
    options:
      path: /etc/grafana/provisioning/dashboards/json
